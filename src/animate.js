export const fadeIn = () => ({
    duration: 400,
    css: (t) => {
        return `opacity: ${t}`
    }
})
export const fadeOut = () => ({
    duration: 400,
    css: (t) => {
        return `opacity: ${t}`
    }
})